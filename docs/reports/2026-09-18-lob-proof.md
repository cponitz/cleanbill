# SPEC-11 — Lob proof report (L2 address-window check · L5 physical proof)

*Started 2026-09-18 with the L1–L3 build (PR "SPEC-11"). This file is the audit trail for the two things the code cannot
settle on its own: that the recipient block sits inside Lob's `top_first_page` window zone, and that five live letters
reach Charlie with a working QR and correct webhooks. Update it in place as each step happens.*

## 1. What the build verified (2026-09-18, sandbox — no Lob key, no network to lob.com)

| Check | Result |
|---|---|
| Letter renders on one page in both variants with the recipient block pinned at `RECIPIENT_TOP_IN = 2.125 in`, `RECIPIENT_LEFT_IN = 0.75 in` (`cleanbill/letters/generate.py`), the disclaimer above it unchanged, and the entity / return-address line in the footer | rendered locally (A and B, six taxing units, a two-line address); `render_letter` now raises if the body would run into the footer |
| Selector, stratification, seed determinism, variant split, resume, guards, Lob client (429 / 4xx / idempotency / multipart fields) | `tests/test_batch.py` — 14 tests, all green, against a recorded-response fake (no network) |
| Lob signature scheme (`Lob-Signature` = hex HMAC-SHA256 of `<Lob-Signature-Timestamp>.<body>`), five-minute skew, forged / tampered / stale rejected | `_shared/webhook_sig_test.ts` — 2 new tests |
| Event → `mail_pieces.status` mapping, `delivered_at`, `returned_to_sender` → suppressed + `mail_returned`, proof pieces never touch a lead | `webhooks/lob_test.ts` — 3 tests |
| Migration chain + `check_schema.sql` (the `mail_pieces` table, unique `lob_id`, both SQL functions) | applied to a scratch Postgres 16; "schema check passed" |
| `/ops` tiles (mailed · delivered · returned), Health "letters" tile, Mail batch table, drawer letter line | `tsc`, eslint and the design lint green; smoke E extended (`kpi-mailed` / `kpi-delivered` / `kpi-returned`, `mail-batches`, `health-letters`) |

**Not verified here, by construction:** `docs.lob.com` and `api.lob.com` are unreachable from the sandbox and no Lob key
exists yet (§7 step 1). The signature header names and the `timestamp.body` scheme are as the spec pinned them; the
verifier accepts a seconds or a milliseconds timestamp so either reading of Lob's docs works. The window zone below is
the open item.

## 2. Address-window check (L2 — run with the **test** key, before the proof)

    # Mac, repo folder, LOB_API_KEY=test_… in .env
    python -m cleanbill.letters.batch --window-check
    # → data/out/batches/window-check/{window-check.pdf, lob-render.pdf, lob-thumb-1.png}

Then look at `lob-thumb-1.png` (Lob's own render of page 1):

1. The recipient block (name + address lines) is the only ink inside the window zone Lob draws for `top_first_page`, and
   the block is wholly inside it.
2. Every mark — the disclaimer at the top included — clears the 1/16 in ink-free border.
3. The letter is one page (Lob did not add a page).

If (1) fails, move `RECIPIENT_TOP_IN` (and, if needed, `RECIPIENT_LEFT_IN`) in `generate.py`, re-run, repeat. If the
disclaimer's top lines collide with the return-address area of the window zone, that is the case for
`--address-placement insert_blank_page` (an extra sheet per piece) — a business call (cost) to put to Charlie, not a
layout change to the disclaimer (CLAUDE.md: those lines are not touched).

**Result:** _pending — paste the thumbnail here as `docs/reports/2026-09-xx-lob-window.png` and note the constants used._

## 3. Test-mode batch (acceptance 3)

    python -m cleanbill.letters.batch --batch test-2026-09-xx --tier 1 --n 3 --seed 1 --send
    # three ltr_… in the Lob dashboard (test), three mail_pieces rows, three leads mailed; the same command again creates nothing
    # reset: eval/reset_mail_batch.sql with :batch = 'test-2026-09-xx'

**Result:** _pending._

## 4. Physical proof (L5)

Webhook registered (Lob dashboard → Webhooks → `https://letrfpwskjbgnyacesgv.supabase.co/functions/v1/webhooks/lob`,
Live, all `letter.*`; secret set with `supabase secrets set LOB_WEBHOOK_SECRET=…`), then:

    LOB_ENABLED=true  # .env, live key
    python -m cleanbill.letters.batch --batch proof-2026-09-xx --n 5 --to-override "Clean Bill Co.|<line1>|<line2>|<city>|TX|<zip>" --send --confirm-footer "Clean Bill Co."

Checklist when the envelopes arrive (SPEC-11 §7 step 7): window shows the full address · QR opens
`https://cleanbillco.com/claim/CB-…` on a phone and the page shows the right property · the code on paper matches ·
disclaimer legible · entity name and return address right · `/ops` → Health → letters shows the proof batch with the
webhook trail `mailed → in_transit → … → processed_for_delivery` · the five content leads are still `new`.

**Result:** _pending — "go" or the fix list._
